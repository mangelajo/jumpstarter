[project]
name = "jumpstarter-driver-nanokvm-usb"
dynamic = ["version", "urls"]
description = "Jumpstarter driver for NanoKVM-USB devices via serial HID and UVC video"
readme = "README.md"
license = "Apache-2.0"
authors = [
    { name = "Mario Parra", email = "rciots@rciots.com" }
]
requires-python = ">=3.11"
dependencies = [
    "anyio>=4.10.0",
    "jumpstarter",
    "jumpstarter-driver-composite",
    "opencv-python-headless",
    "numpy",
    "pillow>=10.0.0",
    "pydantic",
    "pyserial>=3.5",
    "click",
]

[project.entry-points."jumpstarter.drivers"]
NanoKVMUSB = "jumpstarter_driver_nanokvm_usb.driver:NanoKVMUSB"

[tool.hatch.version]
source = "vcs"
raw-options = { 'root' = '../../..'}

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.metadata.hooks.vcs.urls]
Homepage = "https://jumpstarter.dev"
source_archive = "https://github.com/jumpstarter-dev/repo/archive/{commit_hash}.zip"

[tool.pytest.ini_options]
addopts = "--cov --cov-report=html --cov-report=xml"
log_cli = true
log_cli_level = "INFO"
testpaths = ["jumpstarter_driver_nanokvm_usb"]
asyncio_mode = "auto"

[build-system]
requires = ["hatchling", "hatch-vcs", "hatch-pin-jumpstarter"]
build-backend = "hatchling.build"

[tool.hatch.build.hooks.pin_jumpstarter]
name = "pin_jumpstarter"

[dependency-groups]
dev = [
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=6.0.0",
    "pytest>=8.3.3",
]
